Respuestas BETA:
a-Después del merge, todos los commits/cambios quedan en el main.
b-Si.
c-208  git checkout main
  209  git merge centurion
  210  git checkout centurion
  211  git merge main
  212  git fetch origin main
  213  git merge main
  214  git merge --allow-unrelated-histories
  215  git merge main --allow-unrelated-histories
  216  git push
  217  git checkout main
  218  git merge centurion
  219  git push

Responde ALFA:
G- 44  git pull
   45  git checkout zindis
   46  ifconfig >> beta.txt
   47  date >> beta.txt
   48  git add .
   49  git commit -m "Agrego ifconfig y date"
   50  git push

Responde GAMMA:
d-Quedan actualizada la rama de BETA pero no la de GAMMA
e-al hacer merge tuvimos el problema "unleashed histories", creo que este se debia a que estaban como separadas,
tengo entendido que cometi el error de iniciar el repo que se me pidio local como si fuera como un repo nuevo con git init.
f-
git pull
sudo apt install net-tools
date
ifconfig

