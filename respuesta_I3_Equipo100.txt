Respuestas BETA:
a-Después del merge, todos los commits/cambios quedan en el main.
b-Si.
c-208  git checkout main
  209  git merge centurion
  210  git checkout centurion
  211  git merge main
  212  git fetch origin main
  213  git merge main
  214  git merge --allow-unrelated-histories
  215  git merge main --allow-unrelated-histories
  216  git push
  217  git checkout main
  218  git merge centurion
  219  git push

Responde ALFA:
G- 44  git pull
   45  git checkout zindis
   46  ifconfig >> beta.txt
   47  date >> beta.txt
   48  git add .
   49  git commit -m "Agrego ifconfig y date"
   50  git push

Responde GAMMA: 
d-la rama de BETA queda actualizada pero veo que la rama de GAMMA no termina con los mismos archivos que la rama creada por BETA
e-había cometido un error al iniciar el repo descargado como si fuera un repo nuevo con "git init" en vez de clone, y eso genero problemas
de commits que no aparecian.
f-
sudo apt install net-tools
git pull
date
ifconfig
git push
